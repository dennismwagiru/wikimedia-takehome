( function () {
	// TODO: Use Vue / React / Svelte / framework of your choice if that is easier.
	$('a').on('click', function (e) {
		// TODO: Load the article preview and allow updating the article
		// without requiring a browser refresh
	});

    // TODO: The "title" field should autocomplete on existing articles in the system.
}() )
