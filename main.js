'use strict';

( function () {
	// NOTE:
	// You may replace this file with Vue / React / Svelte / JS library of your choice,
	// if you prefer, but that is definitely not required!
	$('a').on('click', function (e) {
		// TODO: When clicking the article title, we'd like to get the article text
		//  and show it in the page without requiring a refresh of the page.
	});

    // TODO: When typing in the "title" field, we want to auto-complete based on article titles
	//  that already exist. You may use the api.php?prefixtitle={search} endpoint for
	// auto-completion. You can use https://api.jqueryui.com/autocomplete/ or another auto-complete
	// widget of your choice.
}() )
